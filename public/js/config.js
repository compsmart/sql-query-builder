/**
 * Configuration for the Query Builder application
 */
const AppConfig = {
    // API endpoints
    api: {
        base: '/api',
        config: '/api/config',
        tables: '/api/tables',
        columns: '/api/columns',
        relationships: '/api/relationships',
        buildQuery: '/api/query',
        executeQuery: '/api/execute',
        discoverSchema: '/api/discover'
    }
};
